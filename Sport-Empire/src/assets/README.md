# `src/assets` directory

Static images and media used by UI components.

## Notes

- Keep original asset names if they are referenced directly in imports.
- Prefer optimized image sizes for better page performance.
